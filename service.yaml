apiVersion: v1
kind: Service
metadata:
  name: task-tracker-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: task-tracker
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080